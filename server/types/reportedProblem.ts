export enum ReportedProblemStatusType {
    pending = "pending",
    unseen = "unseen",
    resolved = "resolved",
}

export enum ReportedProblemCategoryType {
    technical = "technical",
    financial = "financial",
    other = "other",
}
